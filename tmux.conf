bind k select-pane -U
bind j select-pane -D
bind h select-pane -L
bind l select-pane -R
bind b resize-pane -Z

bind c new-window -c "#{pane_current_path}"
bind % split-window -c "#{pane_current_path}"
bind '"' split-window -h -c "#{pane_current_path}"

set -g renumber-windows on
set -g set-clipboard on
set -g status-position top
set -g status-right ""
set -g mouse
setw -g mode-keys vi

set -g status-left-length 50
set -g status-left "#{?client_prefix,#[bg=#89b4fa] #S ,#[fg=#1e1e2e,bg=#cba6f7,bold] #S }"

set -g status-style bg="#11111b"
set -g window-status-format "#[fg=#cdd6f4,bg=default] #I:#W "
set -g window-status-current-format "#[fg=#f9e2af,bg=#1e1e2e,bold] #I:#W #[fg=#cdd6f4,bg=default]"


set -g @plugin "tmux-plugins/tpm"
set -g @plugin "tmux-plugins/tmux-sensible"
set -g @plugin "jimeh/tmuxifier"

run '~/.tmux/plugins/tpm/tpm'
